import globalVariables from "./globalVariables.js";


const keyupEventEnter = globalVariables.searchBar.addEventListener('keyup', (event) => {
    if (event.key === "Enter") {

    }
})

export default keyupEventEnter;